# Webscraper
WebScrap is a lightweight Python script built to test and interact with public or private API endpoints. It helps developers quickly send HTTP GET requests to a given URL and inspect the returned data. The script uses the popular requests library to handle network communication, providing a simple and reliable way to check an API’s response
